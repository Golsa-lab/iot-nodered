/**
 * Classify: ZBEE_ZCL / Link Status / Ignore
 *
 * Body of the Node-RED function node `1e29d592d9dd5ff5` in flows.json,
 * wired with 1 output.
 * It runs inside Node-RED with `msg`, `node`, `flow` and `global` in scope,
 * which is why it reads as a bare body rather than a module.
 *
 * Generated by tools/extract-functions.js — edit the flow, not this file.
 */
var cs      = msg._row['Command String'] || '';
var pktType = msg._row['Packet Type']    || '';
if (cs.indexOf('Layer ZBEE_ZCL') !== -1) {
    msg._type = 'zbee_zcl';
} else if (pktType.indexOf('Link Status') !== -1) {
    msg._type = 'link_status';
} else {
    msg._type = 'ignore';
}
return msg;
