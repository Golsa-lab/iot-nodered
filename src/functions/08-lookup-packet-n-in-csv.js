/**
 * Lookup packet N in CSV
 *
 * Body of the Node-RED function node `3c5af8ad41207730` in flows.json,
 * wired with 1 output.
 * It runs inside Node-RED with `msg`, `node`, `flow` and `global` in scope,
 * which is why it reads as a bare body rather than a module.
 *
 * Generated by tools/extract-functions.js — edit the flow, not this file.
 */
//i will search through packets
var csvData = flow.get('csvData');
if (!csvData) {
    node.error('csvData not loaded.');
    return null;
}
var row = csvData[msg._N];
if (!row) {
    node.warn('Packet ' + msg._N + ' not found');
    return null;
}
msg._row = row;
return msg;
