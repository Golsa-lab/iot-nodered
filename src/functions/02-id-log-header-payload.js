/**
 * id_log header payload
 *
 * Body of the Node-RED function node `7161e8a47cf263b9` in flows.json,
 * wired with 1 output.
 * It runs inside Node-RED with `msg`, `node`, `flow` and `global` in scope,
 * which is why it reads as a bare body rather than a module.
 *
 * Generated by tools/extract-functions.js — edit the flow, not this file.
 */


flow.set('id_counter', 0);
flow.set('received_count', 0);
flow.set('stop_generation', false);
flow.set('outgoing_cost_rows', []);   
flow.set('outgoing_cost_idx', {});   
msg.payload  = 'No.,ID,TIMESTAMP\n';
msg.filename = './challenge3_output/id_log.csv';
return msg;
