/**
 * Split into individual ThingSpeak msg
 *
 * Body of the Node-RED function node `e0d3d4fad1610218` in flows.json,
 * wired with 1 output.
 * It runs inside Node-RED with `msg`, `node`, `flow` and `global` in scope,
 * which is why it reads as a bare body rather than a module.
 *
 * Generated by tools/extract-functions.js — edit the flow, not this file.
 */
// Each item in the sequence becomes its own msg
// The delay node downstream passes exactly 1 message every 20 seconds
var sequence = msg.payload;

for (var i = 0; i < sequence.length; i++) {
    var item = sequence[i];
    node.send({
        payload: item.cost,
        _dst: item.dst,
        url: 'https://api.thingspeak.com/update?api_key=YOUR_THINGSPEAK_WRITE_API_KEY&field1=' + item.cost //used my personal API write key 
    });
}

return null;
